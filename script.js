document.addEventListener('DOMContentLoaded', function() { // Hamburger Menu window.toggleMenu = function() { console.log('toggleMenu called'); const menu = document.getElementById('sideMenu'); const backdrop = document.getElementById('menuBackdrop'); if (menu && backdrop) { menu.classList.toggle('active'); backdrop.classList.toggle('active'); console.log('Menu active:', menu.classList.contains('active')); } else { console.error('Menu or backdrop element not found'); } }; window.closeMenu = function() { console.log('closeMenu called'); const menu = document.getElementById('sideMenu'); const backdrop = document.getElementById('menuBackdrop'); if (menu && backdrop) { menu.classList.remove('active'); backdrop.classList.remove('active'); } }; // Chart Toggle let isChartVisible = false; window.toggleChart = function() { console.log('toggleChart called'); const container = document.getElementById('chartContainer'); const toggleLink = document.querySelector('.toggle-chart'); if (container && toggleLink) { isChartVisible = !isChartVisible; if (isChartVisible) { container.classList.add('active'); toggleLink.textContent = 'Hide Chart ▲'; console.log('Chart shown'); } else { container.classList.remove('active'); toggleLink.textContent = 'Show Chart ▼'; console.log('Chart hidden'); } } else { console.error('Chart container or toggle link not found'); } }; });